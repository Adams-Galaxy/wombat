[package]
name = "wombat"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
publish = false
description = "A Lua-powered dotfiles compiler"
readme = "README.md"
repository = "https://github.com/Adams-Galaxy/wombat"
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
clap_complete = "4.6"
mlua = { version = "0.12", features = ["lua55", "vendored"] }
nix = { version = "0.31", features = ["process", "signal"] }
handlebars = { version = "=6.4.4", default-features = false }
globset = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde-saphyr = { version = "=1.1.0", default-features = false, features = ["deserialize", "serialize"] }
sha2 = "0.11"
similar = "=3.1.2"
tempfile = "3"
thiserror = "2.0"
toml = "1.1"
url = "2"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
